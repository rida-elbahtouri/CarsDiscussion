<% @users.each do |user| %>
    <div class="smallgrid">
    <div>
    <%= link_to  user.fullname, user_path(user),class:"bold"  %>
    </div>
    <div>
    <%= link_to "follow", follow_path(user_id:user.id),class:"btn btn-small btn-info" %>
    </div>
    </div>
<%end%>